CSS Selectors -> This topic is helpful in DOM manipulation cuz html selectors are same as CSS selectors

nodeList and HTMLCollection are array-like structures in DOM
nodeList -> it has forEach()
HTMLCollection -> it does not have forEach

HTMLCollection -> to loop on this, you convert it into array using 👇
                                                                Array.from(HTMLCollection)